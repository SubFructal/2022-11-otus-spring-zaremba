spring:
#  datasource:
#    url: jdbc:h2:mem:testdb

  datasource:
    url: jdbc:postgresql://localhost:5430/demoDB
    username: usr
    password: pwd

  sql:
    init:
#      mode: never
      mode: always
      data-locations: test-data.sql
      schema-locations: app-schema.sql

  jpa:
    generate-ddl: false
#    generate-ddl: true
    hibernate:
      ddl-auto: none
#      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
#        format_sql: true

logging:
  level:
    root: WARN
